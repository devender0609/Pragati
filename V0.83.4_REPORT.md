# v0.83.4 — Product truth + review handoff finalization

Maintenance only. No lesson content changed, no §3.4, no instructional
decomposition, Growth still frozen, review state unchanged.

---

## Baseline (v0.83.3, reproduced)

typecheck 0 errors · **1703 tests / 99 files** · `validate:curriculum`
green · `build` green. Matches what v0.83.3 reported.

---

## 1. The obsolete Student claim, removed

`src/components/OnboardingFlow.tsx`, the first sentence a student read:

> Pragati is a CBSE / NCERT-informed adaptive assessment prototype for
> Class 6 Math. It is built to help a teacher spot misconceptions across
> a small bank of items, focus a follow-up lesson, and plan a short
> classroom intervention.

Untrue twice over: Growth is frozen and was never calibrated, so nothing
here is an adaptive assessment; and Pragati is no longer a Class 6
product.

## 2. Replacement wording

> Pragati helps you learn and practise mathematics from your own
> CBSE / NCERT course. It follows the chapters of your textbook, so what
> you see here matches the book you use in class.

The three bullets now read: **Your book's chapters** (in the order your
textbook teaches them; some have lessons ready, the rest are listed so
you can see what is coming), **Practice with feedback** (a wrong answer
gets the mistake behind it explained), and **Your teacher can see your
work**.

The caveat box now says:

> **Still being built.** Lessons are written for a few chapters so far,
> and the rest of your book is listed but not ready yet. Nothing here is
> a score, a grade, or an official CBSE product, and a teacher checks
> everything Pragati shows.

No claim of adaptivity, validation, norms, RIT equivalence or complete
Classes 1–12 content. The locked Student design is untouched: same
heading, same bullet component, same amber caveat panel.

## 3. Every active product claim, audited and classified

| Occurrence | Class | Action |
|---|---|---|
| `OnboardingFlow.tsx` — "adaptive assessment prototype for Class 6 Math" | **A — active user-facing** | Replaced (above) |
| `ResultsView.tsx` — teacher label "Prototype ability estimate" (students see "Level right now") | B — owner-facing, already hedged | Kept |
| `AssignmentForm.tsx` — "Prototype note: the adaptive engine still uses its current stop rule" | B — teacher-facing, describes a rule-based picker honestly | Kept |
| `LearnView.tsx` — "adaptive item picker", audit trail text | B — admin/audit surface | Kept |
| `AlignmentReviewView.tsx`, `AssessmentPicker.tsx`, `CurriculumCoverageView.tsx`, `ImportedSubmissionsView.tsx`, `JoinClassroomView.tsx`, `ClassroomWorkflowTest.tsx` — "prototype", "pre-pilot", "teacher review required" | B — caveats that understate rather than overclaim | Kept |
| `index.html` title | already corrected at v0.83.2 | — |
| Version reports, historical packages, `docs/review/*` | C — historical | Kept verbatim |
| Tests asserting the old strings are absent | C — regression guards | Kept |

No occurrence of "Growth Assessment Prototype", "validated adaptive",
"RIT", "normed", "growth score" or "all lessons available" survives as a
current claim.

## 4. SEND_THIS contradictions removed

Two in the file a sender follows today:

- The banner said **"v0.83.2 — send the `_CURRENT` folders only."** It
  now names no release and states the full current identity, pointing at
  `CURRENT_REVIEW_IDENTITY_v0_83_3.md` for the table.
- A v0.78 paragraph still said §7.4's package **"is frozen by design and
  is NOT regenerated"**. That stopped being true at v0.83.2, when the
  package was regenerated around the corrected page. The current text
  states the gap plainly and puts the old reasoning in a parenthetical
  marked as history, pointing to
  `HISTORICAL_PROVENANCE_CORRECTION_v0_83_1.md`.

Three current package files also still said "we have built/written one
lesson". Nine Fractions sections are drafted, so each now says so and
names §7.4 as the anchor being reviewed first. The two `IDENTITY.md`
files are retitled "CURRENT FOR SENDING (v0.83.4)" and state that the
package was regenerated rather than frozen.

## 5. Current §7.4 identity — verified from the final code

| | Value |
|---|---|
| Instructional content fingerprint | `dfc56ab5` |
| Review code | `S74-v1-DFC56A` |
| Lesson artifact version | `1` |
| Source provenance fingerprint | `efeccb48` |
| Source provenance version | `2` |
| Source page | `159` |

Printed by the built modules, not copied from the brief; all six match
what the brief expected. Every current handoff and package file agrees,
and a test compares the documents against the live values.

## 6. Historical material preserved

`PRAGATI_SECTION_7_4_REVIEW_FINAL/`,
`PRAGATI_SECTION_7_4_CURRICULUM_REVIEW/`,
`PRAGATI_CHAPTER_3_REVIEW_PACKAGES/`,
`PRAGATI_CHAPTER_7_REVIEW_PACKAGES/` keep page 160, the old codes and
their `DO_NOT_SEND.md`. `HISTORICAL_PROVENANCE_CORRECTION_v0_83_1.md`,
`docs/review/*` and the version reports are unchanged. No current
instruction points at any of them except to say they are historical.

## 7. Review state — unchanged

- **Fractions:** 9 complete · 9 review-ready · **0 sent · 0 reviewed ·
  0 published**
- **Number Play:** 3 complete · 3 aligned · 3 review-ready · **0 sent ·
  0 reviewed · 0 published**

## 8. Runtime curriculum counts — unchanged

Browser-verified again this release, Student and Teacher, at 390 / 768 /
1440: **13, 11, 14, 14, 15, 10, 15, 14, 8, 14, 14, 13**.

## 9. Class 9 — still partial

Eight verified Ganita Manjari Part I chapters; full textbook denominator
**UNKNOWN**; the limitation still stated on both surfaces; no Part II
invented; no CBSE↔NCERT crosswalk.

## 10–18. Verification

| Gate | Result |
|---|---|
| Final tests | **1734 passed / 100 files** (v0.83.3: 1703 / 99) |
| typecheck | 0 errors |
| validate:curriculum | green |
| build | green, 9.63s |
| visual QA | 69 captured / 0 refused |
| interaction matrix | 57 probes / 0 problems |
| 12-class Student + Teacher curriculum QA | **72 rows, 72 pass, 0 null assertions** |
| Review-integrity + product-truth suites | 133 passed |
| Generated-document determinism | identical across consecutive runs |

**New tests (31):** `v0834ProductTruth.test.tsx` renders the real
onboarding component and fails on any forbidden claim, checks the
replacement wording and the incomplete-content caveat, ensures the old
sentence is not merely hidden but gone from rendered strings, and sweeps
every current handoff and package file for a superseded page, a
superseded code presented as current, a "not regenerated" claim standing
as current, or a stale release-state line. Superseded values are still
allowed where the text is explaining that they are superseded.

**Unresolved warnings:** the pre-existing chunk-size warning only —
`index` ~1.41 MB and `main` ~1.28 MB exceed 500 kB after minification.
Not addressed here by instruction. No unresolved imports, no missing
assets, no source maps in `dist`.

**Still open from earlier releases:** Classes 10–12 structure is from
contents pages only; page-level mathematical intent is inspected for the
12 authored Class 6 sections alone; the CG document behind the Class 9
crosswalk has not been read.

No mathematical defect was found while auditing.

---

**STOP.** The next phase — the Classes 1–12 page-level intent audit and
instructional decomposition — has not begun.
