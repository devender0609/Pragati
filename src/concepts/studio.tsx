// v0.77 — the kit moved into the product on integration. Kept as a
// re-export so the concept harness renders the SAME components the
// shipped screens use; a divergence here would mean the thing you
// approved and the thing that ships are two different files.
export * from '../design/Studio';
